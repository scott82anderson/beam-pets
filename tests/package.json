{
  "name": "tests",
  "version": "0.1.0",
  "license": "UNLICENSED",
  "private": true,
  "scripts": {
    "setup": "node ./cypress/setup.js",
    "headed": "npm run setup && npx cypress open --browser chrome",
    "headless": "npm run setup && npx cypress run --headless --browser chrome"
  },
  "dependencies": {
    "cypress": "^8.2.0",
    "cypress-dotenv": "^1.2.2",
    "dotenv": "^10.0.0",
    "got": "^11.8.2",
    "wait-on": "^6.0.0"
  }
}
